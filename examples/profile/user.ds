mode = "dark";

style = "text-black bg-white";
if mode == "dark" {
  	style = "text-gray-200 bg-gray-800";
}

list = ["Jason", "Jack", "Justin"];

return div {
  	class: style,
	for name in list {
		return p { "Hello, ", name, "!" };
	}
};