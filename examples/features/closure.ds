// getSequence will generate a anonymous function:
fn getSequence () {
  let i = 0;
  return fn () {
    i = i + 1;
    return i;
  };
}

let add = getSequence();

print(add());
print(add());
print(add());
print(add());
print(add());
